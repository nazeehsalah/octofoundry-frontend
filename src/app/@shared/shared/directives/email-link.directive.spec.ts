import { EmailLinkDirective } from './email-link.directive';

describe('EmailLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailLinkDirective();
    expect(directive).toBeTruthy();
  });
});
